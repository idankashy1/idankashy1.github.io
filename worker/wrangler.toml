name = "idan-portfolio-chat"
main = "src/index.ts"
compatibility_date = "2024-11-12"

# Comma-separated allowlist of origins that can call this Worker.
# Add your production URL here (GitHub Pages or custom domain) plus the
# Vite dev ports you actually use. Anything not on this list is rejected.
[vars]
ALLOWED_ORIGINS = "https://idankashy1.github.io,http://localhost:5173,http://localhost:5174,http://localhost:5175"

# Secrets (set via wrangler):
#   npx wrangler secret put ANTHROPIC_API_KEY
